<h1>投稿フォーム</h1>

<% if @post.errors.any? %>
  <div id="error_explanation">
    <h4><%= pluralize(@post.errors.count, "エラー") %>が発生しました。</h4>
    <ul>
      <% @post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<%= form_with(model: Post.new, url: root_path, method: 'post') do |f| %>
  <h4>好きなカラオケのジャンル（またはよく歌うカラオケのジャンル）</h4>
  <%= f.select :genre, [["J-POP"], ["アニソン"], ["K-POP"], ["ボーカロイド"], ["演歌"], ["洋楽"], ["その他"]] %>

  <h4>記入欄</h4>
  <%= f.text_area :message %>

  <h4>写真のアップロード</h4>
  <%= f.file_field :photo, accept: "photo/*", required: false %>

  <%= f.submit 'ポストする' %>
<% end %>